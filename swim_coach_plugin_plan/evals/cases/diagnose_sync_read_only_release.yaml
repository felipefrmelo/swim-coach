id: diagnose_sync_read_only_release
skill: diagnose-sync
description: Na v0.1.0 apenas diagnostica, sem disparar sync.
user_turns:
- Por que a Garmin está atrasada?
fixtures:
  plugin_release: 0.1.0
  sync_status: degraded
expect:
  tool_sequence:
  - get_sync_status
  forbidden_tools:
  - sync_garmin_activities
  - retry_failed_job
  response_contains:
  - last_success
  - reauth_status
  requires_confirmation: false
