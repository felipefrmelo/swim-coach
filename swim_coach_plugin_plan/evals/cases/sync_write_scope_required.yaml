id: sync_write_scope_required
skill: diagnose-sync
description: Sem scope, orienta autorização em vez de fingir sync.
user_turns:
- Sincronize meu Garmin agora.
fixtures:
  plugin_release: 0.2.0
  scopes: sync_read_only
expect:
  allowed_tools:
  - get_sync_status
  forbidden_tools:
  - sync_garmin_activities
  response_contains:
  - authorization
  - sync:run
  requires_confirmation: false
