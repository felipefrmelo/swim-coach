# Handoff P03

## Escopo entregue

- P03-T01..T09 implementadas localmente e provadas por fixture, SDK oficial,
  PostgreSQL e UI sanitizada.
- Gate final permanece aberto somente para atividade real/comparação Garmin,
  dependente do gate persistente P02.

## Arquivos relevantes

- `backend/alembic/versions/000005_p03_fit_normalization_analytics.py`: schema P03.
- `backend/src/swim_coach/infrastructure/fit/parser.py`: FIT → domínio versionado.
- `backend/src/swim_coach/application/services/activity_data.py`: pipeline e match.
- `backend/src/swim_coach/domain/activities/metrics.py`: analytics determinísticos.
- `backend/src/swim_coach/interfaces/rest/activities.py`: API privada user-scoped.
- `apps/web/src/routes/activities.tsx`: experiência móvel de análise/feedback.
- `backend/tests/fixtures/p03/`: fixture sanitizado e golden.

## Comandos executados

- `make check` → 87 Python, 2 Vitest, Ruff, mypy, ESLint, TypeScript e 8 checks.
- Playwright Chrome mobile → 6 passed em stack persistente.
- Testcontainers migration + pipeline, build/up Compose e volume 0700 → verdes.
- Pip/pnpm audit e gitleaks → nenhum achado.

## Migrações

- `000005_p03_fit_normalization_analytics.py` sobre `000004`.
- `up/down/up` exercitado em PostgreSQL descartável.

## Contratos alterados

- REST adiciona atividades, detalhe normalizado, enqueue de processamento,
  correção manual e feedback; nenhum campo fornece FIT/checksum/storage key.
- Outbox/job interno adiciona `activity.fetch_file` idempotente.

## Evidências manuais

- Nesta etapa são fixtures sanitizados e integração local, não atividade real.
- A atividade real P00 provou leitura, mas não persistência P02 nem normalização P03.

## Riscos/limitações restantes

- concluir conexão Garmin criptografada e replay P02;
- processar uma natação real e comparar agregados mascarados com Garmin;
- filesystem é adequado ao single-node pessoal; storage distribuído é futuro.

## Próxima ação recomendada

- Injetar a chave P02 pelo ambiente local, executar `connect`, `sync-once` duas
  vezes, processar uma pool swim e registrar somente contagens/métricas sanitizadas.
