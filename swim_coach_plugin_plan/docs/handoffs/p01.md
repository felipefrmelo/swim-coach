# Handoff — P01 Domínio, persistência e identidade

## Escopo entregue

- P01-T01..T03: value objects, erros, entidades de identidade/atleta/metas e
  seed inicial idempotente 20 m / 2.000 m / 45 min.
- P01-T04..T05: PostgreSQL/SQLAlchemy/Alembic, repositories, UoW, jobs, outbox,
  auditoria e idempotência com invariáveis transacionais.
- P01-T06..T07: OIDC BFF/PKCE, sessão opaca, CSRF, allowlist, logout, REST
  user-scoped, Problem Details e health com readiness do banco.
- P01-T08..T09: PWA mobile-first sem chat, segundo usuário fixture e testes
  completos de ownership/IDOR.

## Arquivos relevantes

- `backend/alembic/versions/000001_p01_foundation.py`: schema reversível P01.
- `backend/src/swim_coach/domain/`: entidades/value objects sem dependência de I/O.
- `backend/src/swim_coach/application/services/`: identidade, sessão, OIDC e contexto.
- `backend/src/swim_coach/infrastructure/db/`: mappings e repositories SQLAlchemy.
- `backend/src/swim_coach/infrastructure/auth/`: cliente OIDC com validação JWT.
- `backend/src/swim_coach/interfaces/rest/`: BFF e recursos user-scoped.
- `apps/web/src/`: shell PWA, rotas, API client e estados operacionais.
- `tests/e2e/p01-auth-context.spec.ts`: fluxo mobile de aceite.
- `adrs/ADR-0010-pwa-bff-session.md`: decisão de sessão BFF.
- `docs/evidence/p01-domain-persistence-identity.md`: transcript sanitizado.

## Comandos executados

- `make check` → 39 testes Python, 2 Vitest, Ruff, mypy, ESLint, TypeScript e
  validadores verdes.
- `make dependency-scan` → nenhuma vulnerabilidade conhecida.
- `make secret-scan` → nenhum vazamento no histórico/árvore.
- `docker compose up --build -d` → migration, API, worker e web operacionais.
- `.venv/bin/alembic ... current` no contêiner → `000001 (head)`.
- consulta somente ao catálogo PostgreSQL → 16 tabelas e 55 constraints.
- seed sanitizado duas vezes → mesmo resultado `20/2000/2700/135`.
- `corepack pnpm e2e` com Chrome e viewport 375×812 → 1 fluxo passou.
- smokes `/health/live`, `/health/ready`, auth config e manifest → passaram.

## Migrações

- Nova revision `000001_p01_foundation` cria as 15 tabelas de produto P01; com
  `alembic_version`, o schema público possui 16 tabelas.
- Testcontainers comprova automaticamente `upgrade/downgrade/upgrade` em banco
  descartável. Em banco persistente, criar backup antes de qualquer downgrade.
- O Compose executa migration one-shot e bloqueia API/worker até o sucesso.
- Não há backfill nem dado pessoal em migration; o seed é CLI explícito.

## Contratos alterados

- OpenAPI skeleton ganhou cookie session, CSRF, idempotência e REST P01.
- Eventos de domínio documentam criação/atualização de meta.
- Erros públicos seguem Problem Details e incluem correlation ID.
- A superfície MCP permanece intencionalmente P00/read-only.

## Evidências manuais

- Reais locais: PostgreSQL, migration, imagens Compose, API/worker/Nginx,
  Chrome E2E e health/readiness.
- Fixture: dois usuários `example.test`, dev-auth allowlisted e contexto inicial.
- Contrato criptográfico: discovery/JWKS/ID token OIDC com RSA controlada.
- Não executado/não alegado: login Auth0 de usuário, Garmin, FIT, MCP privado,
  editor de workout ou qualquer efeito externo.

## Riscos/limitações restantes

- OIDC precisa de client/redirect URI configurados no IdP para um smoke externo;
  segredos nunca devem entrar no repositório.
- `dev-auth` é útil somente localmente e o bootstrap já o proíbe em produção.
- O SDK MCP 1.29 exige `model_rebuild` do settings interno devido a uma forward
  reference; a versão está fixada e teste com warnings como erro cobre o ajuste.
- A persistência P01 não contém ainda o modelo de atividades/import da P02.
- O volume Compose atual contém somente fixtures sanitizadas de desenvolvimento;
  não foi apagado automaticamente.

## Próxima ação recomendada

1. Iniciar P02 por `prompts/p02.md` e seu mapa de contexto.
2. Criar migrations aditivas para atividades/import, sem reescrever `000001`.
3. Reutilizar `user_id`, UoW, idempotência, outbox e audit entregues na P01.
4. Manter qualquer chamada Garmin inicialmente read-only e anexar somente
   evidência sanitizada.

## Estado final

`DONE`. Todas as tasks P01-T01..T09 e o gate da fase têm evidência. P02 é a
primeira fase elegível; P04 também depende de P01, mas a ordem canônica continua
P02 antes de P04.
