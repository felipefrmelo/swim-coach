# P12 handoff

## Escopo entregue

- P12-T01..T10 implemented: production hardening, encrypted recovery, privacy,
  observability, security/capacity gates, version 1.0, runbooks and separate
  public-readiness assessment.
- Published as implementation commit `aa98d80`; PR #13 is ready and retargeted
  to `main` as the umbrella integration for the complete linear P00–P12 history.

## Arquivos relevantes

- `backend/alembic/versions/000010_p12_privacy.py`: export/deletion state.
- `backend/src/swim_coach/infrastructure/backup.py`: encrypted backup/restore.
- `backend/src/swim_coach/application/services/privacy.py`: owned export/delete.
- `docker-compose.production.yml`: hardened deployment overlay.
- `ops/alerts.yaml`: operational thresholds.
- `releases/plugin-1.0.0.json`: release hashes, images and gate evidence.
- `docs/runbooks/p12-operations.md`: install/update/rollback/recovery/incident.

## Comandos executados

- `make check` → 126 Python passed in 14.31 s, 4 web passed, lint/types/validators green.
- `pnpm lint/check/test` → lint/typecheck and 4 tests passed.
- focused restore drill → 1 passed in 5.02 s on PostgreSQL 16.
- disposable Compose + load smoke → healthy, 120 requests, p95 82.35 ms.
- dependency scan + Gitleaks → zero findings.
- Syft → 744-component SBOM; Trivy → four images, zero HIGH/CRITICAL.
- `docker compose build` → four images; final users are non-root.
- plugin validator/reinstall → personal 1.0.0 cachebuster enabled.
- GitHub Actions run `31621402450` → complete quality job passed.

## Migrações

- `000010` adds `data_export` and `deletion_request`; PostgreSQL `up/down/up`
  passed and the restored database retained revision `000010`.

## Contratos alterados

- OpenAPI privacy endpoints, error catalog, capability/release matrix and the
  plugin manifest/version were updated.

## Evidências manuais

- User supplied real Garmin read, Auth0 metadata, Tunnel doctor and ChatGPT
  connection outputs; these are sanitized in the P12 evidence file.
- Personal plugin install is real local state. New-thread seven-Skill use remains
  pending because the current thread cannot reload a just-installed plugin.

## Riscos/limitações restantes

- P11 and earlier real-data gates remain open, so P12 stays `IN_PROGRESS`.
- local in-process rate limiting is appropriate for the personal single-instance
  release, not public multi-instance deployment.
- artifact deletion after DB commit may require orphan reconciliation after a
  filesystem failure.

## Próxima ação recomendada

- Open a new Codex/ChatGPT thread, invoke the upgraded plugin read-only, then run
  the documented P11 bounded personal automation/offline/queue proof. Append only
  sanitized counts/screenshots, rerun CI and promote the release candidate when
  every earlier real gate is closed.
