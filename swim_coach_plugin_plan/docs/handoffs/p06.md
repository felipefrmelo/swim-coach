# Handoff P06

## Escopo entregue

- P06-T01..T04 e P06-T06 concluídas no repositório.
- P06-T05 possui marketplace e app real, mas o upgrade da cópia pessoal não foi
  autorizado pelo ambiente dentro do prazo.
- P06-T07/T08 permanecem parciais até conversa nova e gate P05 autenticado.

## Arquivos relevantes

- `plugins/swim-coach/.codex-plugin/plugin.json`: manifesto read-only `0.1.0`.
- `plugins/swim-coach/.app.json`: mapeamento real da conexão registrada.
- `plugins/swim-coach/skills/*`: três Skills e metadados de interface.
- `tests/evals/cases/p06-read-only.yaml`: 66 casos de seleção/orquestração.
- `backend/tests/contract/test_plugin_release.py`: contratos da release/evals.
- `releases/plugin-0.1.0.json`: hashes e gates pendentes.

## Validação executada

- plugin-creator validator → aprovado;
- skill-creator quick validator ×3 → aprovado;
- contrato P06 → 3 testes aprovados;
- gate completo → 94 Python, 2 Vitest, lint, tipos e validadores aprovados;
- dependency scan e gitleaks (17 commits + worktree) → sem achados;
- app mapping comparado à instalação remota gerada pelo host;
- instalação pessoal inspecionada: ativa, ainda `0.0.0-spike`.

## Upgrade recuperável pendente

Executar fora do sandbox, a partir do Git root:

```bash
mv /home/felipe/plugins/swim-coach /home/felipe/plugins/swim-coach.backup-20260812T134108Z
cp -a /home/felipe/projetos/swim_coach/swim_coach_plugin_plan/plugins/swim-coach /home/felipe/plugins/swim-coach
python3 /home/felipe/.codex/skills/.system/plugin-creator/scripts/update_plugin_cachebuster.py /home/felipe/plugins/swim-coach
codex plugin add swim-coach@personal
codex plugin list
```

O rollback é mover a nova cópia para outro nome e restaurar o backup datado.
Não remover o backup antes do smoke.

## Smoke restante

1. reiniciar o app e abrir conversa nova;
2. testar “como foi minha última natação?”;
3. testar “como está meu progresso para a meta?”;
4. testar “por que minha natação não apareceu?”;
5. confirmar que somente tools read-only foram chamadas e nenhuma senha foi pedida;
6. salvar transcript sanitizado e atualizar P06 para `DONE` somente se o token
   Auth0 e os dados Garmin persistidos também satisfizerem o gate P05.
