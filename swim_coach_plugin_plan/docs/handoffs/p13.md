# Handoff P13 — ChatGPT-first

## Estado

Implementação, validação, CI e implantação pessoal concluídas. O conector
ChatGPT precisa ser reconectado para obter o novo scope `coach` e uma conversa
nova precisa executar o smoke autenticado.

## Decisão vigente

ADR-0011 substitui a interface proposal/hash/approve/execute. O contrato público
é v2, usa somente `coach` e oferece oito tools. Segurança de dados e efeitos
externos continua interna.

## Pontos de código

- `CoachCommandService`: save/publish/generate direto;
- `GarminUpsertService`: fila idempotente por treino/revisão/data;
- worker `workout.upsert_garmin`: create/update/reschedule/reconcile;
- `create_mcp_server(..., v2_enabled=True)`: allowlist exata de tools;
- `POST /api/v1/workouts/save`: fluxo auxiliar da PWA;
- `plugins/swim-coach/`: Skills e manifesto 2.0.

## Próximos passos operacionais

1. reconectar o conector ChatGPT;
2. abrir conversa nova e validar listagem/leitura; publicar somente um treino real
   escolhido pelo usuário.

## Evidência operacional

- commits `4bf2cec` e `0887f96` na `main`;
- CI `32966938164` verde;
- quatro serviços saudáveis, API/worker sem reinícios ou erros recentes;
- host 200, login Auth0 303, MCP não autenticado 401;
- scope `coach` aceito pelo Auth0 e oito tools exatas registradas na imagem;
- Garmin ativo com dois dispositivos e zero bindings duplicados.
