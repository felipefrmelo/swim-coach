# Handoff P07

- Tasks P07-T01..T09: implementadas localmente.
- Gate: pendente somente do canário real Garmin descartável.
- Migration: `000004_p07_garmin_write_actions.py` sobre `000003`.
- Provider real usa exclusivamente métodos públicos de `garminconnect==0.3.10`.
- Provider fake é permitido somente fora de produção.
- Preview não chama provider; aprovação exige hash exato e flag de escrita.
- Create e schedule são jobs separados e idempotentes.
- Timeout ambíguo consulta biblioteca/calendário antes de qualquer nova escrita.
- Falta de confirmação passa todos os registros para `NEEDS_RECONCILIATION`.
- Kill switch padrão: desligado; live exige credencial cifrada e título `[CANARY]`.
- Delete/unschedule remoto não foi implementado por segurança.

Validação local: 50 unitários + 14 integração + 2 web; E2E Chrome mobile e build
verdes. Evidências em `docs/evidence/p07-garmin-write-pwa.md`.

Próximo passo exato: executar o runbook real P07 sem compartilhar credenciais,
registrar apenas IDs mascarados/contagens/status e manter o gate aberto se qualquer
efeito não puder ser confirmado.
