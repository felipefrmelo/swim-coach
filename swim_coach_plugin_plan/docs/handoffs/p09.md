# Handoff P09

## Escopo entregue

- P09-T01..T07 implemented locally.
- P09-T08 passed in the Playwright bridge host; real ChatGPT host smoke remains
  pending, so the phase is `IN_PROGRESS`.
- Plugin candidate advanced to `0.3.0` without changing the six Skills or 132
  existing behavioral evals.

## Arquivos relevantes

- `backend/src/swim_coach/interfaces/mcp/ui.py`: resources and card projections.
- `backend/src/swim_coach/interfaces/mcp/assets/swim-coach-card-v1.html`: portable
  self-contained MCP Apps component.
- `backend/src/swim_coach/interfaces/mcp/server.py`: optional render tools.
- `backend/src/swim_coach/settings.py`: fail-closed UI flag.
- `contracts/mcp-tools.yaml`: five public render-tool contracts.
- `contracts/capability-release-matrix.yaml`: P09 tool/resource release mapping.
- `backend/tests/contract/test_mcp_ui.py`: resource, security and headless parity.
- `backend/tests/integration/test_mcp_write.py`: persisted proposal/render boundary.
- `tests/e2e/p09-mcp-apps-ui.spec.ts`: bridge-host mobile/a11y/safety tests.
- `releases/plugin-0.3.0.json`: candidate hashes and pending real gate.

## Migrações

None. P09 projects existing P03/P04/P07/P08 records and adds no persisted state.

## Comandos executados

- `make check` → 106 Python, 2 Vitest, lint, format, mypy and validators passed.
- focused PostgreSQL MCP integration → 1/1 passed.
- focused P09 Playwright/Chrome → 3/3 passed at 375×812.
- `make dependency-scan` and `make secret-scan` → no findings.
- `make build` and wheel inspection → Vite/Compose images built and the HTML
  resource is present in the Python wheel.

## Contratos alterados

- Five P09 render tools are read-only and each points to one versioned resource.
- Existing read/write tools retain their P08 contracts and no UI dependency.
- Proposal decisions call the existing approval tool; execution stays separate.

## Runtime flags

```text
SWIM_COACH_OAUTH_ISSUER=https://<tenant>/
SWIM_COACH_OAUTH_RESOURCE=https://<public-mcp>/mcp
SWIM_COACH_MCP_WRITE_ENABLED=true
SWIM_COACH_MCP_UI_ENABLED=true
```

Turn off only `SWIM_COACH_MCP_UI_ENABLED` for immediate rollback to the exact
headless P08 surface.

## Evidências manuais

- Sanitized bridge-host screenshot can be regenerated with
  `P09_EVIDENCE_SCREENSHOT=docs/evidence/p09-mcp-apps-ui.png` and the focused
  Playwright spec.
- No real ChatGPT P09 screenshot is claimed yet.

## Riscos e limitações restantes

- ChatGPT host compatibility, framing and user interaction still require the
  real connector smoke.
- Earlier P02/P03/P05/P06/P07/P08 real-data and live-write gates remain open.
- UI must never be treated as authority; server ownership, scopes, expiry and
  action hash remain authoritative.

## Próxima ação recomendada

Publish the P09 draft PR, run the real ChatGPT host checklist above when the
OAuth scopes are available, then start P10 independently without marking P09
`DONE` prematurely.
