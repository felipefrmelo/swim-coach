# Handoff — P04 Treino canônico, calendário e editor PWA

## Escopo entregue

- P04-T01/T02: árvore discriminada para step/repeat, condições de término,
  targets, estilos/drills, equipamento/intensidade, totais, warnings e limites.
- P04-T03/T04: revisões imutáveis com SHA-256 canônico, ETag, migration `000003`,
  JSONB versionado, índices, histórico e proteção PostgreSQL contra update.
- P04-T05: REST e serviços user-scoped para criar, revisar, validar, aprovar
  localmente, agendar, listar/detalhar e persistir templates.
- P04-T06/T07: editor touch-first, reordenação, repeats, preset 20 m, totais ao
  vivo, erros, histórico e calendário semana/mês com reagendamento acessível.
- P04-T08/T09: quatro fixtures normativas, JSON Schema, contrato, unit/property,
  integração PostgreSQL e E2E Chrome.

## Arquivos principais

- `backend/src/swim_coach/domain/workouts/`: schema canônico, validador e agregados.
- `backend/src/swim_coach/application/services/workouts.py`: casos de uso sem provider.
- `backend/alembic/versions/000003_p04_workout_authoring.py`: schema reversível.
- `backend/src/swim_coach/interfaces/rest/workouts.py`: REST/ETag user-scoped.
- `apps/web/src/routes/workouts.tsx`: editor, histórico e calendário.
- `examples/workouts/`: técnica, endurance, velocidade e teste.
- `tests/e2e/p04-workout-authoring.spec.ts`: gate móvel.
- `docs/evidence/p04-workout-authoring-pwa.md`: transcript sanitizado.

## Verificação

- `make check` → 60 Python + 2 Vitest e todos os checks estáticos verdes.
- `npm run e2e` com Chrome móvel → 4/4 fluxos verdes.
- `make dependency-scan` e `make secret-scan` → limpos.
- Compose → PostgreSQL/API/worker/web saudáveis na revision `000003`.

## Migração e rollback

- `000003` depende linearmente de `000002` e cria templates, workouts, revisões
  e agenda.
- `downgrade` remove somente estruturas P04; Testcontainers comprovou
  `up/down/up`.
- Em dados reais, fazer backup antes de downgrade. Revisões podem ser excluídas
  com o agregado para atender privacidade, mas não atualizadas in-place.

## Riscos e próximos passos

- P02 ainda precisa do smoke persistente Garmin/replay privado; P03 permanece
  bloqueada por esse gate.
- P04 torna P07 elegível tecnicamente, mas a ordem do plano mantém P03/P05/P06
  antes de escrita Garmin.
- P05 deve reutilizar os DTOs/serviços P04 em modo read-only, sem serializar FIT,
  secrets ou estruturas internas.
- O PR P04 é empilhado sobre P02 para manter uma única cadeia Alembic; respeitar
  a ordem de integração e a autorização explícita de merge.

## Estado final

`DONE`. Todas as tasks P04-T01..T09 e o gate estão cobertos por evidência local
real e fixtures claramente identificadas. Implementação publicada no commit
`82e9e77184df4c0a57a6e5125737a022d46b9b59` e no draft PR #4.
