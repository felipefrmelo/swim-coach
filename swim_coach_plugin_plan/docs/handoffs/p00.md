# Handoff — P00 Fundação e spikes de risco

## Escopo entregue

- P00-T01: bootstrap do workspace, lockfiles e comandos padronizados.
- P00-T02: ambiente Compose descartável e healthchecks.
- P00-T03: skeleton modular, API e worker sem lógica futura fictícia.
- P00-T04: MCP Streamable HTTP local com somente `get_capabilities`, validado
  por testes, MCP Inspector, sessão Codex real e Secure MCP Tunnel no ChatGPT web.
- P00-T05: plugin Skills-only `0.0.0-spike`, marketplace pessoal e instalação
  real habilitada no Codex.
- P00-T06: probe seguro de OAuth metadata/PKCE implementado e testado; execução
  em tenant Auth0 real passou para authorization code, PKCE S256, DCR e resource
  binding; o `tunnel-client doctor` descobriu o metadata path-aware com HTTP 200.
- P00-T07: probe Garmin read-only e modelo local de natação implementados; login
  e leitura reais passaram sem escrita externa.
- P00-T08: gates locais e GitHub Actions run `31515474864` verdes.
- P00-T09: decisões, limitações, evidências e decisão final `DONE` registradas.

## Arquivos relevantes

- `pyproject.toml`, `uv.lock`: runtime Python e dependências fixadas.
- `package.json`, `pnpm-lock.yaml`, `apps/web/`: workspace e shell PWA sem chat.
- `backend/src/swim_coach/`: limites application/interfaces/bootstrap/worker.
- `backend/scripts/probe_oauth_metadata.py`: validação pública sem tokens.
- `backend/src/swim_coach/interfaces/rest/oauth.py`: protected resource metadata
  configurável, ausente por 404 quando o par HTTPS não está configurado.
- `backend/scripts/probe_garmin_read.py`: probe interativo read-only com cleanup.
- `plugins/swim-coach/`: manifesto e Skill mínimo P00.
- `.agents/plugins/marketplace.json`: marketplace de teste versionado.
- `../.codex/config.toml`: conexão MCP project-scoped com allowlist do tool P00.
- `docker-compose.yml`, `backend/Dockerfile`, `apps/web/Dockerfile`: stack local.
- `../.github/workflows/ci.yml`: pipeline na raiz Git real.
- `tools/validate_repository.py`: validação dos artefatos da implementação.
- `docs/evidence/p00-foundation-evidence.md`: transcript e distinção real/pendente.

## Comandos executados

- `make check` → passou; 21 testes backend e 1 frontend, além de lint,
  typecheck e validadores.
- `make dependency-scan` → passou sem vulnerabilidades conhecidas.
- `make secret-scan` → passou no histórico atual e na árvore de trabalho.
- `docker compose build` → imagens API, worker e web construídas.
- `docker compose up -d --wait` → quatro serviços operacionais/saudáveis.
- `docker compose down -v` → stack removido e volume PostgreSQL descartável apagado.
- `curl` nos endpoints live/ready/web → respostas `ok`/`ready`.
- MCP Inspector `tools/list` e `tools/call` → um tool, chamada estruturada,
  `isError=false`.
- `codex mcp get swim_coach_p00` → conexão Streamable HTTP e allowlist reconhecidas.
- `codex exec --ephemeral --sandbox read-only ...` → chamada real de
  `swim_coach_p00.get_capabilities`, `status=completed`, `error=null`.
- validator do `plugin-creator` → plugin válido.
- `codex plugin add swim-coach@personal` → instalado/habilitado como
  `0.0.0-spike`.
- `probe_garmin_read.py` → 20 atividades recentes, 6 nados em piscina, 2
  dispositivos e Forerunner 265 detectado; `external_write_performed=false`.
- `tunnel-client doctor --profile swim-coach-p00 --explain` → `RESULT ok`; MCP
  local alcançável e chave de runtime lida somente por variável de ambiente.
- ChatGPT web via Secure MCP Tunnel → invocação real de `@coach` descreveu o
  protótipo P00 e a única função `get_capabilities`.
- `probe_oauth_metadata.py --issuer <Auth0 real>` → authorization code, PKCE
  S256 e DCR válidos, sem obter ou imprimir token.
- rebuild do Compose → API, worker, PostgreSQL e web saudáveis; a nova rota
  respondeu 404 sem configuração, preservando o comportamento fail-closed.
- rebuild com issuer Auth0 real e resource loopback →
  `/.well-known/oauth-protected-resource/mcp` retornou HTTP 200 e vínculo exato.
- rerun OAuth-focused do `tunnel-client doctor`, com chave fictícia e control
  plane loopback → `oauth_metadata PASS` no metadata path-aware; o doctor com
  runtime key real e a chamada ChatGPT anteriores provam o transporte externo.
- probe OAuth completo com `--allow-loopback-http` → `oauth_probe=passed`,
  `resource_binding=true`, PKCE S256 e DCR, sem obter token.
- [GitHub Actions run `31515474864`](https://github.com/felipefrmelo/swim-coach/actions/runs/31515474864) → job `quality` verde em 1m05s.

## Migrações

- Nenhuma. Persistência de produto e schema de domínio permanecem fora da P00.
- O volume PostgreSQL usado no smoke test é descartável e não contém dados reais.
- O volume foi removido ao final; seu conteúdo vazio não é recuperável, mas o
  ambiente pode ser recriado integralmente pelo Compose.

## Contratos alterados

- Adicionado `GET /.well-known/oauth-protected-resource`, habilitado somente com
  `SWIM_COACH_OAUTH_ISSUER` e `SWIM_COACH_OAUTH_RESOURCE` configurados.
- Adicionado o endereço path-aware
  `GET /.well-known/oauth-protected-resource/mcp`, usado pelo tunnel para o MCP
  montado em `/mcp`; HTTP é aceito apenas em loopback fora de produção.
- `get_capabilities` implementa o envelope 1.0 e a matriz de capacidades P00.
- Nenhuma `.app.json` foi criada: a conexão de desenvolvimento foi registrada
  na superfície hospedada de Tunnel e não forneceu um ID de app para inventar no manifesto.

## Evidências manuais

- Reais: stack Docker saudável, health endpoints, MCP Inspector local, chamada
  por sessão Codex, plugin pessoal instalado/habilitado, Garmin read, Auth0
  discovery e Secure MCP Tunnel chamado pelo ChatGPT web.
- Teste local: construção do modelo de natação de 20 m sem upload.
- O probe OAuth não emitiu token; validação JWT user-scoped continua corretamente
  reservada para P05, antes da primeira tool com dados privados.

## Riscos/limitações restantes

- Auth0 DCR está habilitado no tenant de desenvolvimento real; por ser registro
  aberto, o tenant deve permanecer isolado e com permissões mínimas.
- `python-garminconnect` é não oficial e pode quebrar com mudanças do Garmin.
- O login Garmin recebeu 429 no caminho mobile antes de o fallback concluir; o
  provider precisa preservar backoff e classificação de rate limit.
- Um tunnel temporário não substitui a URL HTTPS estável exigida pelo produto.
- A invocação remota atual comprova apenas a superfície pública P00; dados
  privados continuam desabilitados.
- O remote GitHub responde por SSH; commit `2faaf62`, PR #1 e CI remota verde
  comprovam publicação e execução em clone limpo.
- `codex mcp list` revelou que outro MCP global preexistente guarda uma credencial
  estática nos argumentos do processo. O valor não foi copiado; ele deve ser
  rotacionado e migrado para uma variável de ambiente antes de novos logs.

## Próxima ação recomendada

1. Iniciar P01 pelo algoritmo de `LLM_START_HERE.md` e `prompts/p01.md`.
2. Preservar o tunnel e o tenant de desenvolvimento apenas se forem necessários
   nos gates seguintes; caso contrário, parar o processo e revogar a runtime key.
3. Não habilitar dados privados até P05 validar token, audience e scopes em cada chamada.

## Estado final

`DONE`. Todas as tasks P00-T01..T09 têm evidência. P01 é a primeira fase
`NOT_STARTED` com dependências `DONE` e pode ser iniciada em uma nova execução.
