# Handoff P05

## Escopo entregue

- P05-T01..T06 e P05-T09 implementadas e validadas localmente.
- P05-T07 coberta pelo cliente MCP automatizado; execução no MCP Inspector pendente.
- P05-T08 e o gate final no ChatGPT/Codex permanecem pendentes.

## Arquivos relevantes

- `backend/src/swim_coach/infrastructure/auth/mcp.py`: JWT/JWKS resource server.
- `backend/src/swim_coach/application/services/mcp_read.py`: queries e DTOs mínimos.
- `backend/src/swim_coach/interfaces/mcp/server.py`: tools, scopes, schemas e erros.
- `backend/alembic/versions/000006_p05_mcp_observability.py`: invocations sanitizadas.
- `backend/tests/integration/test_mcp.py`: protocolo, authz, IDOR, zero business write
  e performance.
- `backend/tests/contract/test_mcp_tools.py`: contrato e annotations.

## Comandos executados

- `make check` → 91 Python, 2 Vitest, lint/typecheck e validadores verdes.
- Testcontainers MCP/migration/regressões → verdes, incluindo `up/down/up`.
- Testes JWT/JWKS, metadata e redaction → verdes.
- Compose rebuild/up → stack saudável e Alembic em `000006`.
- dependency scan e gitleaks (15 commits + worktree) → nenhum achado.

## Migrações

- `000006_p05_mcp_observability.py` sobre `000005`.
- Downgrade remove índices e tabela de invocations; `up/down/up` exercitado.

## Contratos alterados

- O catálogo `contracts/mcp-tools.yaml` já era normativo e não precisou mudar.
- Protected resource metadata agora anuncia apenas os seis scopes P05 read-only.
- Sem OAuth completo, o servidor registra somente o `get_capabilities` P00.

## Evidências manuais

- Nesta etapa são fixture sanitizada e cliente MCP real in-process, não host OpenAI
  nem token Auth0 real.
- Evidência detalhada: `docs/evidence/p05-mcp-read-only.md`.

## Riscos/limitações restantes

- concluir P02 e P03 com atividade real persistida;
- obter token Auth0 com `aud` igual ao resource MCP e scopes mínimos;
- executar tools/list/call no MCP Inspector e a pergunta no ChatGPT/Codex;
- P06 ainda precisa materializar Skills e pacote `0.1.0` read-only.

## Próxima ação recomendada

- Com credenciais mantidas apenas no ambiente local, concluir os dois syncs P02,
  processar uma pool swim e então executar Inspector + host com token user-scoped,
  salvando somente transcript e métricas sanitizados.
