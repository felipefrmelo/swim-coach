# Handoff P11

## Escopo entregue

- P11-T01..T08 implementation and automated gate are present.
- Scheduler, existing import pipeline orchestration, proposal-only weekly job,
  four notification states, safe PWA cache, offline feedback queue, retention,
  metrics and operations UI were delivered.
- The phase remains `IN_PROGRESS` because its three mandatory personal/manual
  proofs have not been captured.
- Published as commit `f379551` in draft PR #12, stacked on
  `p10-adaptive-planning`.

## Arquivos relevantes

- `backend/src/swim_coach/application/services/automation.py`: timezone and
  dedupe scheduler plus reminders and retention.
- `backend/src/swim_coach/interfaces/worker/main.py`: planning draft execution and
  redacted failure notification.
- `backend/alembic/versions/000009_p11_automation.py`: notification storage.
- `backend/src/swim_coach/interfaces/rest/operations.py`: metrics, inbox and safe
  idempotent retry.
- `apps/web/public/sw.js`: narrow offline cache policy.
- `apps/web/src/offline/feedbackQueue.ts`: IndexedDB queue/reconciliation.
- `apps/web/src/routes/operations.tsx`: queue and notification UI.
- `docs/runbooks/p11-job-operations.md`: recovery and rollback.

## Comandos executados

- clean GitHub Actions run `31617300086` → 119 Python and four web tests,
  lint/types/scans/validators plus four image builds green in 1m40s.
- focused integration suite → 20 PostgreSQL/Testcontainers tests green.
- `make build` → Vite bundle and four Compose images built.

## Migrações

- `000009` adds `notification`, unique `(user_id, dedupe_key)` and recent-inbox
  index; Testcontainers proved `up → down → up`.

## Contratos alterados

- activity feedback now requires `Idempotency-Key`;
- OpenAPI adds operations metrics, safe retry and notification endpoints;
- no MCP or plugin capability/version change is introduced by P11.

## Evidências manuais

- None claimed. Automated evidence uses sanitized identities and disposable real
  PostgreSQL, not the personal runtime or iPhone host.

## Riscos/limitações restantes

- IndexedDB feedback retained after a semantic 409 needs human reconciliation;
- browser service-worker updates follow normal browser lifecycle and can briefly
  retain the preceding safe cache version;
- scheduler is intentionally a personal-scale database scan, not a commercial
  multi-tenant cron platform.

## Próxima ação recomendada

Publish this branch as a draft PR, then start P12 without marking P11 `DONE`.
Later run the three personal proofs in `docs/evidence/p11-automation-offline.md`.
